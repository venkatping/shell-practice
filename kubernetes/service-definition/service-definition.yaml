apiVersion: v1
kind: Service
metadata:
   name: myapp-service
spec:
   type: NodePort
   ports:
      - targetPort: 80 #port on the pod
        port: 80  #port on the service
        nodePort: 30008   #port on the node
   selector:
     app: myapp
     type: front-end
